import Navbar from "../../components/Navbar";

const NewGroup = () => {
    return (
        <section>
            <Navbar path={"/"} title={"new-group"} />
            <div>New Group</div>
        </section>
    );
};

export default NewGroup;
