import Navbar from "../../components/Navbar";

const Chat = () => {
    return (
        <section>
            <Navbar path={"/"} title={"Chat"} />
            <div>Chat</div>
        </section>
    );
};

export default Chat;
